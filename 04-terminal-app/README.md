# NodeJS

## Installing dependencies

1. Install the last stable version

```bash
npm install packageName
```

2. Install the last dependencies version using @latest

```bash
npm install packageName @latest
```

3. Install the next dependencies version using @next

```bash
npm install packageName @next
```

4. Install a specific version number

```bash
npm install packageName@version_number
```
