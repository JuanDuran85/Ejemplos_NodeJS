<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Nueva
    body
        form(action="/tareas", method="post")
            textarea(name="descripcion", cols="30", rows="10", placeholder="Escribir aqui la descripción...")
            br
            input(type="submit", value="Guardar")