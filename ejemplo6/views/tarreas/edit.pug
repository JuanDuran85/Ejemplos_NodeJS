<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
        h2 Editando la tarea N°: #{tarea.id}
        form(action=`/tareas/${tarea.id}?_method=PUT`, method="post")
            textarea(name="descripcion", cols="30", rows="10")= tarea.descripcion
            br
            input(type="submit", value="Actualizar")